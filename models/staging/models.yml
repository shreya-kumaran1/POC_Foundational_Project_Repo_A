version: 2

models:
  - name: stg_customers
    columns:
      - name: c_custkey
        tests:
          - not_null
          - unique
        constraints:
          - type: primary_key

  - name: stg_orders
    columns:
      - name: o_orderkey
        tests:
          - not_null
          - unique
        constraints:
          - type: primary_key

